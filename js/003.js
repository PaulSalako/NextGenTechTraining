// This Create a box to store our first number (10)
const num1 = 10;
// This Create a box to store our second number (5)
const num2 = 5;

// This Add the two numbers together and store the result
const add = (num1 + num2);
// This Show the result of adding the numbers
console.log(add);

// This Subtract num2 from num1 and store the result
const sub = (num1 - num2);
// This Show the result of subtracting the numbers
console.log(sub);

// This Divide num1 by num2 and store the result
const divide = (num1 / num2);
// This Show the result of dividing the numbers
console.log(divide);